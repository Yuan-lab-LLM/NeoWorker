# Design audit — 智能体与日常入口

## Captured flow

1. 点击侧栏“智能体”
   - Capture: `audit-agents-hub.png`
   - Health: poor. 侧栏按钮被选中，但主区域仍展示“任务中枢”，用户无法确认自己已进入哪个对象。
2. 点击侧栏“日常”
   - Capture: `audit-everyday-agent.png`
   - Health: blocked. “日常”处于选中状态，但主区域落在通用欢迎/对话页，而不是日常智能体的运行台。

## Evidence-backed findings

- [P0] 两个一级入口没有稳定地带到它们所命名的对象。
  - Evidence: 两张 Electron 截图中，侧栏状态已经变化，主内容分别仍为任务中枢与通用欢迎页。
  - Impact: 用户无法建立“我在哪、它和另一个入口有什么区别”的基本心智模型。
  - Fix: 先修复导航状态和内容容器的一致性；选中态必须与对应页面一一匹配。

- [P1] “智能体”在侧栏的真实路由是工作区主页，而不是代码中已有的 `AgentsHubPanel`。
  - Evidence: `App.tsx` 将 `onOpenAgents` 指向 `setCurrentView("home")`，而实际智能体中心仅在 `currentView === "agents"` 时渲染。
  - Impact: 菜单名称与行为不符；“日常智能体”看起来像另一个重复产品，而不是智能体体系的一部分。
  - Fix: 将侧栏“智能体”改为“工作区”并保持其主页语义；为 `AgentsHubPanel` 提供真正的“智能体团队”入口。

- [P1] “日常”只说名称，没有解释其专属职责。
  - Evidence: `EverydayAgentPanel` 管理能力范围、触发信号、审批、暂停、回执和可信例行任务；它是一个受策略约束的主动协作操作员，而非全部智能体的列表。
  - Impact: 用户会误解它与“智能体团队”是并列、重复的两个概念。
  - Fix: 改名为“主动协作”或“日常协作”，并用副说明明确“一个会观察、建议并在批准后执行的日常智能体”。

- [P2] 目前日常页面的视觉目标不清晰。
  - Evidence: 当前落地页是通用欢迎/对话页，没有正在观察什么、下一项建议、等待审批、可信例行任务的可视重点。
  - Impact: 即便导航修复，用户也看不出日常智能体的价值和安全边界。
  - Fix: 将页面重做为“主动协作工作台”：顶部状态与今日工作摘要、左侧待你决定的事项、中央执行脉络、右侧边界与已授权能力；不要使用泛泛的营销欢迎区。

## Recommended information architecture

- 工作区：对话、文件、项目和任务的日常入口。
- 智能体团队：角色型/托管型智能体的目录、分工和运行状态。
- 主动协作：唯一的日常智能体；展示它正在观察的信号、准备的建议、需要审批的动作和可审计回执。
- 自动化：显式配置的规则与定时流程。

## Visual direction for the rebuilt 日常 page

- 首屏只回答三个问题：它现在在做什么、下一步需要我决定什么、它被允许做什么。
- 以一条“今日协作脉络”取代大欢迎语：已观察 → 已形成建议 → 等待确认 → 已交付。
- 用真实的回执、建议、例行任务和能力边界组成页面；空状态应告诉用户怎样连接邮箱、日历、文件或浏览器来启动协作。
- 视觉上采用安静的工作台层级：一块明确的状态主卡、两列内容区、低对比边界和少量状态色；避免首页式广告横幅和堆砌卡片。

## Accessibility risks visible from capture

- 仅靠颜色和选中背景表达导航状态时，当前主内容未变化会使键盘和读屏用户更难判断上下文。
- “日常”过于抽象，缺少可读的副标题或 aria 说明；应明确为“主动协作（日常智能体）”。

## Limits

- 本次审查验证了侧栏状态与可见内容的错位；未执行可能改变策略、审批或数据的日常智能体动作。
